---
description: 查询与索引核查流程；数据库类型和索引选型由使用者定义
globs:
  - "**/*Mapper.xml"
  - "**/*.sql"
  - "**/job/**"
alwaysApply: false
---

# 查询与索引核查

这是验证流程，不是数据库选型或建索引配方。数据库引擎、表规模、热表、索引命名和变更窗口由使用者填写；不预设 MySQL/InnoDB 或某种列顺序。

## 触发与范围

写 Mapper、原生查询、CREATE TABLE，或修改 Job/批量扫描时加载。核查不因轻量或单点而豁免，也不因此升档。SQL 写在 Java 注解、查询构造器里时同样适用，不能只依赖 glob。

必须核查：列表过滤、时间窗口、JOIN 的 ON/WHERE、排序分组、业务唯一键和批量扫描。已确认的主键/唯一键等值点查、ID 回查或规模明确的小字典可以简化，须说明依据；不是看到“小表”字样就自动跳过。

## JOIN 正确性

1. 从结构、业务文档或同类实现确认关联键及业务含义，不凭同名字段猜关联。未知关系先取证；仍不能确定再询问。
2. 说明一对一、一对多或多对多，确认连接键是否唯一。检查连接后是否放大行数，导致分页、COUNT、SUM 或去重结果改变；不能用 DISTINCT 掩盖未知关系。
3. 核对 INNER/LEFT 等连接类型是否符合“无匹配行”的业务预期，检查 NULL，以及条件放在 ON 或 WHERE 后是否改变保留行的范围。
4. 只在业务确有租户、用户或其他数据边界时，按项目来源核对关联两侧范围；不预置过滤字段和规则。
5. 用本次相关样例验证：无匹配、单匹配、多匹配、空值；关联含义变化同步 Wiki 和数据库字典。

## 查询与索引核查步骤

1. 列出每张表的 ON、WHERE、ORDER BY、GROUP BY、分页及唯一性要求，记录大致数据量、调用频率与返回规模。
2. 旧表先查已有表结构和字典中相关段落；新表从本次实际查询与唯一约束反推需求。逐项写出“查询条件 → 已有索引 → 覆盖判断与证据”。
3. 结合实际引擎核查复合索引列顺序、等值/范围条件、排序分组及表达式是否可利用索引。最左前缀等判断应注明适用的索引类型；不把某个引擎的经验当通用定律。
4. JOIN 分别核查关联两侧的过滤和连接访问路径，不假定固定驱动顺序，也不因 ON 出现一个字段就机械建索引。计划可用时结合估算/实际行数核对；未取得计划明确标为静态核查。
5. 区分业务唯一约束和性能索引。新增唯一约束先确认重复值、NULL 语义及历史数据处置；性能索引先比较已有覆盖和冗余、写入成本，不按字段名或未来猜想建索引。
6. 发现扫描、排序或连接放大时，给出优化方案或接受理由；不把所有全表扫描一律判错。批量操作需有规模与内存上界，避免无评估全量载入或逐条查询。
7. 热表新增索引先明确锁、耗时、部署窗口和恢复方式。证据不足先完成静态核查，列出缺口；数据库写操作另需明确授权，不能为了取得计划擅自执行有副作用的语句。

## 输出与交付

结论必须是：已覆盖（附依据）、需要变更（附支撑查询）、接受扫描（附理由）或待确认（附证据缺口）。不只写“已核对索引”。

索引变更交给 `java-version-delivery.mdc` 按项目迁移方式交付，并同步 Wiki。交付列出表、索引名、列、支撑查询和验证范围。`workflow/scripts/sql/lint-index-ddl.sh` 只提示 DDL 差异，不验证 JOIN 正确性或执行计划。

## 项目约定（Agent 整理）

| 项目 | 项目约定 / 证据 |
|---|---|
| 数据库引擎和版本 | 待补充 |
| 表结构与查询计划的来源 | 待补充 |
| 热表、数据规模与查询预算 | 待补充 |
| 索引命名、选型与变更窗口 | 待补充 |
| 结构交付和回滚方式 | 待补充 |

结构和关系变化同步实际业务模块文档及数据库字典。是否使用版本 SQL 检查器，按项目明确选择。

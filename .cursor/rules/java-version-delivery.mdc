---
description: 数据结构交付核查；迁移工具、目录和版本策略由使用者填写
globs:
  - "**/*.sql"
  - "**/entity/**"
alwaysApply: false
---

# 数据结构交付约定

## 通用核查

结构变化需识别兼容影响、数据迁移、部署顺序与恢复办法，并同步相关代码和 Wiki。恢复可以是回滚或项目认可的前向修复，不能从通用工作流假定只有一种形式。

不要擅自修改已经交付的迁移记录；按实际项目的版本管理策略核对。只生成文件不等于已执行数据库操作，执行另需明确授权。

## 与查询、索引及 Wiki 的衔接

- 查询和 JOIN 的正确性、索引覆盖按 `java-index.mdc` 核查；不能只提交迁移文件而跳过核查。
- 新表、新增/修改索引：记录支撑查询或唯一性依据，再按本项目方式交付结构变更及恢复方案。
- 关联变化：确认关联键、基数和结果语义，按 `java-wiki.mdc` 同步关系说明；不把关系核查等同于必须新增物理外键。
- 表、字段、索引变化：同步实际数据库字典，注明验证依据及尚未取得的运行证据。

本文件负责“怎样交付结构变化”；索引与 JOIN 的具体核查步骤在 `java-index.mdc`，两者命中时同时读取。

## 项目约定（Agent 整理）

| 项目 | 项目约定 / 来源 |
|---|---|
| 迁移工具或交付方式 | 待补充 |
| 文件路径与命名 | 待补充 |
| 版本归属与已发布文件策略 | 待补充 |
| 部署顺序与恢复方式 | 待补充 |
| 结构字典和验证命令 | 待补充 |

不预设分支名为版本号、固定 SQL 目录、同文件回滚、plain SQL 或特定迁移框架。

## 可选检查器

`workflow/templates/version-sql.md` 保留一种“版本目录 + 正向/回滚段 + 冻结”的交付示例及配套检查器，**默认不启用**。只有使用者选择该格式后才在 config 的 packs 中启用 `version-sql` 并填写实际目录、逻辑库和目标版本。此时该示例的检查才成为门禁。

读取本文件不等于启用示例。已经使用其他迁移机制的项目应配置自己的检查，不套用示例约束。

---
description: 【L1 内核】判轻重、用户零操作、收尾；不含业务表名。领域口径见 AGENTS.md 已启用 packs
alwaysApply: true
---

# Agent 工作流（内核）

不含表名、不含平台细节。本仓库启用了哪些包、知识库路径、P0 扫描名单，以 `AGENTS.md` 为准。**Codex** 无 glob 自动注入，加载时机见 `AGENTS.md` §IDE 并存。

覆盖顺序：**内核 → 已启用 packs → `.cursor/rules/custom/`（团队补全）→ `.cursor/rules/local/`（用户私有）**。后层只追加；**禁止**用 custom/local 关掉 pack 的硬门禁（本包即 既有参数文档保护和启用的 Wiki/SQL 检查）。

## 用户零操作（强制）

**用户只描述需求。** 开任务、阅读、改文件、收尾扫描由 Agent 做。禁止让用户选档、跑脚本、记额外命令、手动 @ 文档。

## 任务轻重（收到需求后第一件事）

**自判，禁止问用户选档。** 按「把这句话做完最少要动什么」。拿不准选更轻的一档；做到一半不够再升级。

默答 5 问（答得出就开干，答不出才问**业务事实**）：

| # | 问 | 用来干什么 |
|---|----|------------|
| 1 | 交付物是什么？（配置 / 数据 / 接口 / 修 bug） | 认清目标，禁止加戏 |
| 2 | 最少改几个文件能交差？ | 文件数是最硬的信号 |
| 3 | 会改受保护结构吗？（表结构 / 表关联） | 若 `AGENTS.md` 启用了对应 pack，才写 schema/wiki |
| 4 | 会动受保护数据吗？ | 若启用了对应 pack，**立刻**按该 pack 过滤；**不因此升档** |
| 5 | 会新增被代码引用的配置吗？ | 按项目配置读取约定核对引用与交付 |

未启用领域规则时仍识别结构和数据风险，但不要臆造该领域的过滤、常量或交付要求。写非主键 SQL 另走 pack **java-index**（核对索引覆盖，不升档）；不是第 6 问，不参与判档。

三档（**只认这一句**；其它文档对照表必须与此相同，禁止另写「动真钱 / 动钱升档」）：

| 档 | 一眼看懂 | 做 | 明确不做 |
|----|----------|----|----------|
| **轻量** | 只动配置或数据，不改代码行为 | 只改当前项目的配置/数据落点（路径见 AGENTS）；展示 diff；收尾见本节「收尾」 | 不读路由、不写专题 wiki、不跑 `finish-audit.sh` |
| **单点** | 就改这一块代码（含改已有 SQL、改已有 Job、单模块 bugfix） | 读 1～2 个同类文件后改那些文件；收尾见本节「收尾」 | 不为流程完整新建 wiki / 跑全套交付清单 |
| **完整** | 跨模块（一次改 ≥2 个业务模块）/ 新增对外接口（HTTP/服务调用/MQ）/ 改表结构或表关联 / **新增** Job Handler | handoff → 阅读集 → 领域样板 → 完整收尾 → 按审计清单输出《变更审计》；P0/P1 清零后输出《最终交付清单》 | 不要因为改了已有 SQL 或已有 Job 就升档 |

**档位不豁免硬门禁**：扫描项以项目 lint 脚本为准。动受保护数据仍可以是单点，但 pack 过滤不能省。写非主键查询仍可以是单点，但索引覆盖核对不能省（不因此升档）。第 3 问为「是」且启用 Wiki 规则时立刻 Read `java-wiki.mdc`（即使没改 `wiki/**`，Cursor glob 也不会自动注入）。

升级只升不一次做满。需求含糊（关联怎么 JOIN、受保护数据范围、热表补索引、循环要不要进内存）→ **停下来问业务**，不问档位。

**防膨胀**：用户没要的文件是不是在「补齐」？同类存量怎么做的？——「补齐 / 流程完整」→ 删掉。

## 开任务（Agent 自动）

开发/排查：先执行 `workflow/task-start.md` 的判档部分。轻量随后只做必要改动；单点读 1～2 个同类实现；完整任务先检查 handoff，再读最小阅读集和场景路由。

纯问答、无改动可以跳过开任务与收尾。不要建立额外命令入口；提交只按用户明确要求进行。

## 编码原则

与平台包冲突时：**内核 + 领域 pack 优先**；仅改平台核心模块时再读平台包。

1. **先理解再动手**：Read 存量实现；完整档再读路由
2. **最小正确 diff**：只改必需文件与行；禁止顺手 refactor
3. **目标驱动**：轻量看数据 diff；单点/完整看 compile / lint
4. **遵守项目约定**：查询实现与编码方式由项目规范决定；评估正确性、性能和变更影响，不规定固定 SQL 形状
5. **不确定先问**：关联无文档、受保护数据范围不明、热表补索引的锁/算法不明、循环要不要进内存拿不准 → 问业务，不问档位
6. **证据先于结论**：有文件变更先跑 lint；完整档再跑项目一键收尾
7. **注释保护**：禁止从**仍保留的方法**上删既有 Javadoc、`@param`、步骤注释。删除整个方法时，注释随方法一起删，不算违规。长编排写步骤注释；临时诊断内容在完成验证后评估是否保留。
8. **受保护数据不过档**：启用了对应 pack 时，写查询当时就按该 pack 过滤；禁止用档位跳过
9. **索引覆盖不过档**：写非主键查询当时按 pack **java-index** 核对，不升档。禁止为「以后可能用到」加索引。热表补索引先问。
10. **新功能按全新落地**：用户说重做 / 新功能 / 没有旧的 → 禁止兼容别名、预留字段、未调用重载。没有明确需求不要增加调度限制。删未调用代码，不要收成 private 留着。
11. **循环加载先选型**（Job / 批量）：写循环前先估次数、每次是否打库、结果集大概多大。**禁止无评估地全量进内存，也禁止无评估地按主体逐条查库。**
    - 配置、本批名单、千级字典 → 循环外一次加载
    - 循环内按主体逐条查库 → 一次批量 SQL / JOIN / 分组聚合
    - 热事实表禁止全表灌 Heap，表名单见 pack **java-index**
12. **异步与定时任务**：核对失败可见性、重试与重复执行影响；具体窗口和恢复策略按业务约定，不从工作流推断。

## 收尾（有文件变更时；权威，其它文档只许指针到此表）

| 档 | 收尾 |
|----|------|
| **轻量** | `lint-agent-diff.sh` + `check-precipitate.sh` + 展示 diff。有新增/变更 HTTP 路径 → `lint-http-paths.sh`；diff 含索引 DDL（含未跟踪的版本 SQL）→ `lint-index-ddl.sh`。命中写入给用户的收尾消息。不跑 `finish-audit.sh` |
| **单点** | 轻量收尾 + 相关模块 compile；有版本 SQL → `lint-version-sql.sh` |
| **完整** | `finish-audit.sh`（已含上两者及 Wiki 格式检查）→ 按审计清单输出《变更审计》；P0/P1 清零后输出《最终交付清单》（含 接口文档 / 索引节） |

**构建顺序**：共享同一构建工作区或 `target` / `build` 产物的 test、compile、clean 必须串行；前一进程退出后再启动下一项。仅产物目录完全隔离的副本可并行，禁止让 clean 删除另一项检查正在使用的产物。

**语义核查不由标签开关决定**：Agent 根据用户需求、实际 diff、必要上下文和同类实现，判断是否涉及任务调度、查询/批量、结构/关联、接口/数据边界，再加载命中的全部规则。脚本的 TAGS 只是已识别线索；没有 job/entity/mapper 标签不能作为跳过依据。无需为每次改动扫描全部领域，只核对实际行为。完整任务在变更审计记录“行为 → 规则 → 依据与检查结论”；轻量、单点保留简要依据。

SEMANTIC_REVIEW=pending 是脚本无法代做的 Agent 核查项，不是自动检查失败，也不能当成已通过。完成阅读和核查后由 Agent 在交付中记录结果；不改脚本输出冒充核验，不按此字段自动委派其他 Agent。

脚本路径见 AGENTS。禁止擅自 `commit`/`push`。Git 提交标题和正文使用中文，说明实际改动与原因；代码标识、路径和项目已有的 feat/fix 等类型前缀可保留原文。例如 `fix: 修复看板预连接阻塞`。只有用户明确要求时才执行提交，不为此新增 Skill、提交脚本或 Git hook。不设 git hook；CI 管不到的远程仍靠本工作流跑 lint。

## 自动沉淀

有信号才往项目 inbox 追加一条（路径见 AGENTS）。无信号则跳过。禁止自填 P0/P1/P2。

信号：用户纠正口径/JOIN/鉴权/数据范围；或规则/脚本拦不住的差点写错。

格式：`- YYYY-MM-DD：现象。草稿：应怎样。案例：路径。同类：slug。定级：待人工。`

收尾跑沉淀检查脚本。同一 `同类` ≥2 条仍待人工 → 对用户警示。禁止空写、重复已沉淀、脚本已能扫的再记。

## 跨会话

仅完整且预计多轮时维护知识库 `.agent/<slug>.md`。
